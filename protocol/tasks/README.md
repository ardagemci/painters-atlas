# protocol/tasks/

One directory per objective (`PIG-001`, `PIG-002`, ...), created at runtime
when the Coordinator opens a task — see `protocol/PROTOCOL.md` §7 for the
required layout. Do not pre-create task directories, and do not hand-edit
Claude's runtime team-state or mailbox directories.
