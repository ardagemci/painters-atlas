# Pigment — Artwork Image and Rights Audit (W-003)

Generated 2026-08-22 against commit `9eca5d5`, via:

```
python3 tools/rights_register.py --surface catalog \
    --out-json protocol/runs/2026-08-22-W-003-9eca5d5.json \
    --out-md   protocol/runs/2026-08-22-W-003-9eca5d5.md
```

**This audit reaches no legal conclusion.** Per owner decision OD-5, it
records what Wikimedia Commons *asserts* about each file; a clearance claim
requires qualified human review. **No previous run of this writ is recorded
on main** (see W-003's "Previous run" section) — this is the baseline, not a
comparison. A future run should diff against these numbers.

## Scope

The **catalog** surface only: the 282 of the catalog's 350 records whose
`image.status` is `"pd"`, per the writ's example invocation. The `gallery`
surface (`js/artworks.js`) and `museum` surface (`js/museums-1.js` venue
photographs) were **not** checked this run — `tools/rights_register.py`
supports both via `--surface gallery` / `--surface museum` / `--surface all`,
but running them was outside the writ's example command and this run's
turn/diff budget. Whether W-003 should broaden to those surfaces is a
decision for whoever reviews this branch, not something this run assumed.

## Summary

| Measure | Count |
| --- | --- |
| Catalog `pd`-status artworks checked | 282 |
| Resolved cleanly by Commons | 282 |
| Unresolved — transient failure (proves nothing) | 0 |
| Missing — Commons API reports the file gone | 0 |
| Attribution required per Commons | 7 |
| Declared `image.page` is NOT the Commons file page | 258 |

Asserted licences: Public domain (267), CC0 (8), CC BY-SA 4.0 (3),
CC BY 3.0 (2), CC BY 2.5 (1), CC BY 2.0 (1).

## Findings, ranked by consequence (per writ W-003)

**1. Unresolvable file — none.** All 282 shipped catalog images with
`status:"pd"` resolved cleanly against Commons: no timeouts, no 429s, and the
API returned no explicit "missing" page for any of them.

**2. Licence changed — not assessable as a *change*; reported as the current
state.** No prior W-003 run exists to diff against. Every licence Commons
currently asserts for these 282 files is an open/free basis consistent with
shipping as `"pd"` (public domain, CC0, or an attribution-style CC licence);
none asserts something incompatible, such as non-commercial-only or
all-rights-reserved.

**3. Attribution now required — 7 files, and all 7 are already credited.**
These are the catalog images whose asserted licence is CC BY* / CC BY-SA*
rather than public domain or CC0. Cross-checked against
`js/photo-credits.js`'s `window.IMAGE_CREDITS` (keyed by Commons file title):
all 7 are present, with author and licence matching what Commons asserts
today. Zero gaps found.

| Catalog id | Commons file | Licence | In `IMAGE_CREDITS`? |
| --- | --- | --- | --- |
| `black-fuji` | File:Katsushika_Hokusai,_tempesta_sotto_la_vetta,... | CC BY 3.0 | yes |
| `pieta` | File:Pieta_de_Michelangelo_-_Vaticano.jpg | CC BY 2.5 | yes |
| `david` | File:'David'_by_Michelangelo_Fir_JBU004.jpg | CC BY 3.0 | yes |
| `triumph-of-death` | File:Triumph_of_Death_Brueghel.jpg | CC BY-SA 4.0 | yes |
| `little-dancer-aged-fourteen` | File:Degas_Little_Dancer_PMA(05c)_(15675423180).jpg | CC BY 2.0 | yes |
| `vahine-no-te-tiare` | File:Paul_gauguin,_vahine_no_te_tiare_(la_donna_coi_fiori),... | CC BY-SA 4.0 | yes |
| `the-ten-largest-no-9` | File:4_hilma_af_klint,_the_ten_largest,_no_9.jpg | CC BY-SA 4.0 | yes |

**4. Declared page is not a Commons file page — 258 of 282 (91%).** 257 of
these are the shape the writ and `tools/commons_rights.py`'s module docstring
already describe: the catalog's `image.page` points at an English Wikipedia
*article*, which carries no licence statement, rather than
`commons.wikimedia.org/wiki/File:...`. This run independently verified the
licence via the Commons API using the file itself, not the declared page, so
the licence findings above are not affected — but the rights basis for those
257 records remains unverifiable from the page URL Pigment actually ships.

One further record, `man-in-a-red-turban`, is a **false positive of the
string comparison, not a real mismatch**: its declared page and the resolved
Commons file page are the same file, differing only in whether parentheses
are percent-encoded (`%28...%29` vs literal `(...)`) — both resolve to
`File:Jan_van_Eyck_-_Portrait_of_a_Man_(Self_Portrait?)_1433.jpg`. Recorded
here rather than silently excluded from the count above.

**5. Nothing changed — not assessable in the literal sense.** No prior
W-003 run exists on main to compare against (see writ, "Previous run"). What
can be said plainly: no image is dead and no licence is unusable in this
snapshot. These 282 resolutions are the number a future run should be
measured against.

## Full per-entry evidence

The complete 282-row table (one row per catalog `pd` artwork, every field
Commons returned) is not committed in this report — at ~320 markdown rows
plus JSON, it would exceed this writ's `max_diff: 400` ceiling on its own.
`protocol/runs/2026-08-22-W-003-9eca5d5.json` instead carries the run
summary plus the 7 flagged entries above (attribution-required or not
cleanly `asserted-by-commons`). The full table is deterministic and cheap to
regenerate on demand:

```
python3 tools/rights_register.py --surface catalog
```
