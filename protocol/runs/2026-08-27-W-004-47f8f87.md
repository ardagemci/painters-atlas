# W-004 run report — 2026-08-27 (base 47f8f87)

## What this run found

`python3 tools/check_image_pages.py --list` at base commit `47f8f87` reported:

```
public-domain catalog images: 330
  cite a Commons file page:   37
  cite something else:        293
```

293 offenders across nine catalog files (`catalog-6.js` through `catalog-9.js`
included, discovered by the `js/catalog-*.js` glob rather than hand-listed).
This is up from the 257 W-003 found and the 293 the writ's own body quotes as
"a day later," confirming the finding's premise: the count is a moving target
because new catalog records keep arriving with a Wikipedia article in
`image.page`, not a fixed backlog.

The previous ledger line for this writ (`branch
lane3/W-004-20260827T100109Z`, `run_id 2026-08-27T10:06:02Z-W-004`) reports a
clean, successful run — but Lane III never merges, so that branch's catalog
edits never reached `main`; only its ledger line did. This run started from
what `main` actually has at `47f8f87`, not from that claim, and confirms the
two are consistent with each other: a real prior fix exists on an unmerged
branch, and `main` still needs the same 293 records repaired until the owner
merges one of these branches.

## Method

1. `python3 tools/rights_register.py --surface catalog --out-json <scratch>` —
   resolved all 330 public-domain catalog `image.src` values against the
   Commons API (`commons_rights.py`, standard rate-limit discipline: ≥0.25s
   between requests, retry-with-backoff on transient failure). Result:
   **330/330 resolved, 0 unverified, 0 missing.**
2. Cross-referenced the 293 `check_image_pages --list` offenders against the
   register by record `id` (verified unique across the whole catalog — no
   collisions). All 293 had a non-empty `commons_file_page` from the register.
3. For each offending record, replaced only the `page:"…"` string inside that
   record's own `image:{…}` block — located by slicing the file between
   consecutive `{ id:"…"` record starts, then matching the *exact* previous
   `page:` string exactly once within that record's slice before writing — so
   no other field or record could be touched by construction.
4. `tools/audit_artworks.py` was never invoked.

## Result: full migration, nothing left unresolved

All 293 offenders resolved and fixed. **Zero records could not be resolved —
there is no "could not fix" list this run.**

| File | Records fixed |
| --- | --- |
| catalog-1.js | 145 |
| catalog-3.js | 52 |
| catalog-4.js | 42 |
| catalog-5.js | 18 |
| catalog-6.js | 12 |
| catalog-7.js | 8 |
| catalog-8.js | 10 |
| catalog-9.js | 6 |
| **Total** | **293** |

(`catalog-2.js` had no offenders — its public-domain records already cited
Commons file pages.)

## Sample — before / after (one per touched file)

| id | file | before | after |
| --- | --- | --- | --- |
| `the-calling-of-saint-matthew` | catalog-1.js | `en.wikipedia.org/wiki/The_Calling_of_Saint_Matthew` | `commons.wikimedia.org/wiki/File:Caravaggio_—_The_Calling_of_Saint_Matthew.jpg` |
| `the-birth-of-venus` | catalog-3.js | `en.wikipedia.org/wiki/The_Birth_of_Venus` | `commons.wikimedia.org/wiki/File:Sandro_Botticelli_-_La_nascita_di_Venere_-_Google_Art_Project_-_edited.jpg` |
| `the-bellelli-family` | catalog-4.js | `en.wikipedia.org/wiki/The_Bellelli_Family` | `commons.wikimedia.org/wiki/File:Edgar_Degas_-_The_Bellelli_Family_-_Google_Art_Project.jpg` |
| `the-tortoise-trainer` | catalog-5.js | `en.wikipedia.org/wiki/The_Tortoise_Trainer` | `commons.wikimedia.org/wiki/File:Osman_Hamdi_Bey_-_The_Tortoise_Trainer_-_Google_Art_Project.jpg` |
| `liberty-leading-the-people` | catalog-6.js | `en.wikipedia.org/wiki/Liberty_Leading_the_People` | `commons.wikimedia.org/wiki/File:La_Liberté_guidant_le_peuple_-_Eugène_Delacroix_-_Musée_du_Louvre_Peintures_RF_129_-_après_restauration_2024.jpg` |
| `bal-du-moulin-de-la-galette` | catalog-7.js | `en.wikipedia.org/wiki/Bal_du_moulin_de_la_Galette` | `commons.wikimedia.org/wiki/File:Renoir,_Pierre-Auguste_-_Dance_at_Le_Moulin_de_la_Galette,_1876.jpg` |
| `portrait-of-omai` | catalog-8.js | `en.wikipedia.org/wiki/Portrait_of_Omai` | `commons.wikimedia.org/wiki/File:Joshua_Reynolds_-_Portrait_of_Omai.jpg` |
| `the-miracle-of-the-slave` | catalog-9.js | `en.wikipedia.org/wiki/Miracle_of_the_Slave_(Tintoretto)` | `commons.wikimedia.org/wiki/File:Tintoretto_-_Miracle_of_the_Slave.jpg` |

The full 293-entry before/after list is reconstructable from `git diff` on
this branch — every changed line is a `page:` line, nothing else moved.

## Records left unresolved

None. 293/293 resolved and fixed.

## Records not in scope, left untouched

The register flagged 1 additional catalog record whose declared
`image.page` already matches the `https://commons.wikimedia.org/wiki/File:`
pattern (so `check_image_pages` correctly does not count it as an offender)
but which points at a *different* Commons file page than the one its live
`image.src` currently resolves to — likely a rename/redirect on Commons since
the record was last touched. This writ's scope is "cites a non-Commons page";
this record already cites a Commons page, so it was left untouched rather than
treated as in scope. Noted here so it isn't lost: re-run
`rights_register.py --surface catalog` and check
`declared_page_is_commons_file_page` if this needs following up later — it is
not one of the 293 counted or touched by this run.

## Diff shape

- Files touched: `js/catalog-1.js`, `js/catalog-3.js` through `js/catalog-9.js`
  — all within `may_write: js/catalog-*.js`.
- 293 insertions / 293 deletions — one line replaced per record; no
  reformatting, no reordering, no whitespace touched on untouched lines.
  Verified: every changed line in every touched file contains `page:` and no
  other line changed (`git diff -U0 | grep -v 'page:'` returns empty across
  all nine catalog files).
- `js/catalog-2.js` untouched (no offenders in it), and every other production
  path (`js/artists-*.js`, `js/artworks.js`, `js/taxonomy.js`, `js/app.js`,
  `css/**`, `index.html`, `docs/**`, `tools/**`) untouched.
- Public-domain catalog record count: 330 before, 330 after — unchanged.
- `tools/audit_artworks.py` was not invoked.

## Verifiers

| Command | Exit |
| --- | --- |
| `osascript -l JavaScript tools/validate.jxa.js` | 0 |
| `python3 tools/validate_agent_system.py` | 0 |
| `python3 tools/check_image_pages.py --require-commons` | 0 |

`check_image_pages` after the fix:

```
public-domain catalog images: 330
  cite a Commons file page:   330
  cite something else:        0
```

## Outcome

Clean. All 293 offenders resolved and fixed; verifier suite green; no field
other than `image.page` changed; no abort condition met. Branch pushed for
owner review; not merged.
